#!/bin/bash
#
#
# This script is a collection of functions to deal with csv files
#
#

find_column() {
    head -1 $filename | awk -v col=$col -F "$separator" '{for(i=1; i<=NF; i++) {			if ( col == $i ) print i}	
}'
} 

